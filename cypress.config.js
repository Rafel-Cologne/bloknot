// ============================================================================
// cypress.config.js
// ============================================================================
// Конфигурационный файл для Cypress
//
// Этот файл содержит настройки для инструмента Cypress:
// - Адрес базового URL приложения
// - Параметры просмотра (viewport)
// - Таймауты
// - И другие опции
// ============================================================================

const { defineConfig } = require('cypress');

module.exports = defineConfig({
  // ========================================================================
  // ОСНОВНЫЕ НАСТРОЙКИ
  // ========================================================================

  e2e: {
    // Базовый URL для всех тестов
    // Когда мы пишем cy.visit('/'), Cypress откроет:
    // http://localhost:5000/
    baseUrl: 'http://localhost:5000',

    // Таймаут ожидания элемента (в миллисекундах)
    // Если элемент не появился за 10 секунд, тест падает
    defaultCommandTimeout: 10000,

    // Таймаут для cy.request() и cy.visit()
    requestTimeout: 10000,

    // Таймаут для response (ответа от сервера)
    responseTimeout: 10000,

    // Включаем видеозапись при ошибках
    video: true,

    // Директория для сохранения видео
    videosFolder: 'cypress/videos',

    // Директория для сохранения скриншотов
    screenshotsFolder: 'cypress/screenshots',

    // Поддерживаемые браузеры для тестирования
    supportFile: 'cypress/support/e2e.js',

    // Spec файлы (тесты)
    specPattern: 'cypress/e2e/**/*.cy.js',

    // Загружаем environment переменные из .env файла
    env: {
      // Адрес приложения
      BASE_URL: 'http://localhost:5000'
    }
  },

  // ========================================================================
  // НАСТРОЙКИ ПРОСМОТРА (VIEWPORT)
  // ========================================================================

  // Размер окна браузера по умолчанию
  viewportWidth: 1280,
  viewportHeight: 720,

  // Размер шрифта по умолчанию
  viewportHeight: 720,

  // ========================================================================
  // ДРУГИЕ НАСТРОЙКИ
  // ========================================================================

  // Папка с файлами поддержки
  supportFile: 'cypress/support/e2e.js',

  // Отчёты об ошибках
  reporter: 'junit',
  reporterOptions: {
    outputDir: 'cypress/reports',
    outputFileName: 'test-results.xml'
  }
});
